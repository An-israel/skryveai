import { ComingSoon } from "@/components/shared/ComingSoon";
import { ClipboardList } from "lucide-react";
export default function Applications() {
  return <ComingSoon title="Applications" description="Talent: track all your proposals in one place. Client: manage your job's incoming applications with status tracking." icon={ClipboardList} />;
}
