import { ComingSoon } from "@/components/shared/ComingSoon";
import { Bell } from "lucide-react";
export default function Notifications() {
  return <ComingSoon title="Notifications" description="All your platform notifications — applications, offers, messages, project updates and more." icon={Bell} />;
}
