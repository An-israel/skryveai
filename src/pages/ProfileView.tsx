import { ComingSoon } from "@/components/shared/ComingSoon";
import { User } from "lucide-react";
export default function ProfileView() {
  return <ComingSoon title="Profile" description="Public profile view — talent portfolio, work history, reviews, and contact." icon={User} />;
}
