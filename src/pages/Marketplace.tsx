import { ComingSoon } from "@/components/shared/ComingSoon";
import { Store } from "lucide-react";
export default function Marketplace() {
  return <ComingSoon title="Marketplace" description="Browse talent and post projects. AI matches the right talent to the right jobs. Full hiring flow — offers, contracts, delivery, payments." icon={Store} />;
}
